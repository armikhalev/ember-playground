
import { englishDetector } from 'em-mela/helpers/english-detector';
import { module, test } from 'qunit';

module('Unit | Helper | english detector');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = englishDetector([42]);
  assert.ok(result);
});

