<h2>This is my awesome Translator</h2>
{{#list-filter
   filter=(action 'filterByWord')
   as |w|}}
    <br>
    <br>
    <ul class="results">
        {{#each w as |word|}}
            <li>{{word.id}} => {{word.english}} : {{word.spanish}}</li>
        {{/each}}
     </ul>
    {{!--{{w.english}} : {{w.spanish}}<br>--}}
{{/list-filter}}
{{!--{{#list-filter
   filter=(action 'filterByCity')
   as |rentals|}}
  <ul class="results">
    {{#each rentals as |rentalUnit|}}
      <li>
        <article class="listing">
        <img src="{{rentalUnit.image}}" alt="">
        <h3>{{rentalUnit.title}}</h3>
        <div class="detail owner">
            <span>Owner:</span> {{rentalUnit.owner}}
        </div>
        <div class="detail type">
            <span>Type:</span> {{rentalUnit.type}}
        </div>
        <div class="detail location">
            <span>Location:</span> {{rentalUnit.city}}
        </div>
        <div class="detail bedrooms">
            <span>Number of bedrooms:</span> {{rentalUnit.bedrooms}}
        </div>
        </article>
</li>
    {{/each}}
  </ul>
{{/list-filter}}--}}

{{!--<br>
<div>^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^</div>
    {{#each model as |word|}}
        {{english-to-spanish word=word}}
        <a>----------------------------------</a>
        {{english-detector word.language}}
    {{/each}}--}}

{{outlet}}

